# Motore deterministico
numpy>=2.0,<3
pandas>=2.2,<3
# NB: Python 3.9.0 ha un bug di typing che rompe la generazione dello schema
# OpenAPI con pydantic >= 2.10 (errore "_SpecialForm has no attribute replace").
# Su Python 3.9.0 resta su 2.9.x. Con Python >= 3.9.1 / 3.11+ puoi usare pydantic>=2.10.
pydantic>=2.9,<2.10

# Strato agentico (Agno + Claude)
agno>=2.6,<3
anthropic>=0.40

# API / mock FE
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0
httpx>=0.27
